# Environment Variables Template for Local Development

# Server (Go API)
PORT=7777
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=secret
DB_NAME=paesi
ALLOWED_ORIGINS=http://localhost:5173
JWT_SECRET=your-secret-key-change-in-production

# Auth Server (Bun)
# PORT=3000 (uses same PORT variable)
# DB_* variables same as above
# ALLOWED_ORIGINS same as above

# Web App (React + Vite)
# Note: For Vite, these should be prefixed with VITE_
VITE_API_URL=http://localhost:7777
VITE_AUTH_URL=http://localhost:3000
